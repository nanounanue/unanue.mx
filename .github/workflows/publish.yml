# Retired Quarto publish workflow. The Quarto scaffold lives in
# archive/quarto/; the ox-hugo pipeline replaces it (see tools/ and
# justfile). This file stays manual-only so it never runs on push; the
# Hugo deploy workflow will replace it when the theme lands.
on:
  workflow_dispatch:

name: Quarto Publish (retired)

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain retirement
        run: echo "This workflow is retired. The site builds with ox-hugo + Hugo; see the justfile."
